import React from "react";
import PropTypes from "prop-types";

import styles from "./$name.module.scss";

const $name = () => {
  return (
    <>
      <h1>$name</h1>
    </>
  );
};

$name.defaultProps = {};
$name.propTypes = {};

export default $name;
